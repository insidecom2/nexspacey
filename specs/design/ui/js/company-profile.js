const COMPANY_ID = 'c1';
const COMPANY_MEMBERS = [{ name: 'ปาริชาติ วัฒนะ', email: 'parichat@baan.software', role: 'owner' }, { name: 'สมชาย ใจดี', email: 'somchai@baan.software', role: 'member' }];
function renderMembers() { document.getElementById('members-list').innerHTML = COMPANY_MEMBERS.map((member) => `<div class="row-between wrap gap-3"><div><p style="font-weight:600;">${member.name}</p><p class="text-sm text-muted">${member.email}</p></div><span class="badge badge-${member.role === 'owner' ? 'primary' : 'neutral'}">${member.role === 'owner' ? 'Owner' : 'Member'}</span></div>`).join(''); }
document.addEventListener('DOMContentLoaded', () => { const company = Store.getCompany(COMPANY_ID); ['name', 'location', 'industry', 'about'].forEach((key) => { document.getElementById('company-' + key).value = company[key]; }); renderMembers(); document.getElementById('company-form').addEventListener('submit', (event) => { event.preventDefault(); ['name', 'location', 'industry', 'about'].forEach((key) => { company[key] = document.getElementById('company-' + key).value.trim(); }); Toast.show('บันทึกข้อมูลบริษัทแล้ว', 'success'); }); document.getElementById('add-member-btn').addEventListener('click', () => Toast.show('การเชิญสมาชิกจะเชื่อมกับอีเมลและ authorization ใน MVP application', 'default')); });
